# TEST FILE — NOT PRODUCTION CONFIGURATION
# Purpose: compare GitHub AI-detected secrets against a custom regex-based
# scanning rule for cnp-plum-recipes-service. All "secret" values below are
# fabricated dummy strings, not real credentials, and are not used by any
# running code path. Safe to delete once the scanning comparison is complete.

# --- Expected TRUE POSITIVES (hardcoded secrets) ---
DB_PASSWORD="qX7!mR2vLp9#zT4wKd8sYf1Hb6nJc3Vg"
DB_PASSWORD_2="!&nwpirn7h4f6&g6eh390daj*YW83hdo"
DB_PASSWORD_3="rT9$kLp4!Wq2#nZ6vXm8&hB3dJf1yC5"
STAGING_API_TOKEN="a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0"
SMTP_PASSWORD='N0tR3al-Sup3rS3cr3tPassw0rd_2024'

# --- Expected TRUE NEGATIVES (should NOT be flagged) ---

# Variable reference, not a literal secret
PASSWORD=${DB_PASSWORD}

# Secret store reference (Terraform-style), not a literal secret
KEY_VAULT_PASSWORD="${data.azurerm_key_vault_secret.plum_db_password.value}"

# Generated at runtime via command substitution, not a literal secret
GENERATED_PASSWORD=$(openssl rand -base64 32)

# Variable name contains "PASSWORD" but holds a non-secret value
PASSWORD_MIN_LENGTH=12
PASSWORD_POLICY_NAME="default-complexity-policy"

